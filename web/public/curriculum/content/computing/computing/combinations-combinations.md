# combinations

<table>
<tr><th>Label</th><th>Formula / Concept</th><th>Description</th></tr>
<tr><td>subsets</td><td>each item is included or not</td><td>efficiency is $O(2^{n})$</td></tr>
<tr><td>subsets</td><td>binary representation</td><td>$101$ three items in or not</td></tr>
<tr><td>subsets</td><td>check if modulo $2^{n}$ is $1$</td><td>to see if $n$ th item is in or not</td></tr>
<tr><td>subsets</td><td>$\lfloor\frac{5}{2}\rfloor$</td><td>removes last item largest digit</td></tr>
<tr><td>permutations</td><td>total possible orders of all items</td><td>efficiency is $O(n!)$</td></tr>
<tr><td>permutations</td><td>next_permutation function</td><td>next in lexicographical order</td></tr>
<tr><td>permutations</td><td colspan="2">while (next_permutation(p, p.size()) { // current permutation is stored in vector p }</td></tr>
</table>
